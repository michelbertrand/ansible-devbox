---
- name: Converge
  hosts: all
  become: true
  vars:
    mysql_root_password: "Molecule_Test_Pw1!"
  pre_tasks:
    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600
  roles:
    - role: mysql
