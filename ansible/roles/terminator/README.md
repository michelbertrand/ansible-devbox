# Terminator Role

Installs Terminator terminal emulator.

## Variables

- `terminator_version`: Version to install (default: "present" = latest)

## Usage

```yaml
- role: terminator
```

## Idempotency

Role is fully idempotent. Running multiple times is safe.
