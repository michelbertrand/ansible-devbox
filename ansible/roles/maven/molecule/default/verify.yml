---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Check mvn binary exists
      ansible.builtin.stat:
        path: /usr/bin/mvn
      register: mvn_bin

    - name: Assert mvn is installed
      ansible.builtin.assert:
        that: mvn_bin.stat.exists

    - name: Run mvn version
      ansible.builtin.command: mvn -version
      changed_when: false
